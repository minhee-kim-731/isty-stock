# ISTY 재고 데스크 — 인수인계 문서

이 파일은 **Claude Code 가 이 폴더를 열 때 자동으로 읽습니다.**
새 계정·새 컴퓨터에서 이어받는 세션은 이것만 읽어도 바로 작업할 수 있게 썼습니다.

사용자 이름은 **민희**입니다 (이메일이 minheekim731 이라 "미니"로 잘못 읽기 쉬운데 틀린 이름입니다).

---

## 1. 무슨 프로젝트인가

민희님이 운영하는 **스페인 온라인 K-뷰티 스토어 ISTY** 의 재고·출고 대시보드입니다.

```
실리콘투(폴란드 SKO Sp. z o.o.)에서 발주
        ↓  제품이 스페인 창고로 입고
민희님이 주문 등록 (한국에서)
        ↓
3PL 파트너 "실장님"(ISTY, 스페인)이 포장·발송
        ↓
재고 차감
```

**두 사람이 서로 다른 나라·다른 시간대에서 같은 데이터를 봅니다.**
한국(민희님)과 스페인(실장님)은 7시간 차이라, 한쪽이 자는 동안 다른 쪽이 일합니다.
그래서 "누가 무엇을 했는지"가 화면에 남는 것이 이 앱의 핵심입니다.

**민희님이 가장 중요하게 여기는 가치는 "잘못했을 때 되돌릴 수 있는가"입니다.**
기능을 추가할 때는 반드시 되돌릴 방법을 함께 만드세요.

---

## 2. 지금 상태

| | |
|---|---|
| 배포 주소 | https://isty-stock.isty-stock.workers.dev |
| 스택 | Cloudflare Workers + D1 (무료 요금제) |
| Cloudflare 계정 | minheekim731@gmail.com |
| D1 위치 | WEUR(런던) — 실장님 응답 속도 우선 |
| 접속 코드 | `ACCESS.local.md` (git 제외) |
| 운영 데이터 | 제품 39종 / 재고 306개 / 주문 0건 (테스트 주문은 전부 감춤 처리) |

**실제로 쓰이고 있습니다.** 프로덕션 데이터를 건드리는 작업은 반드시 백업 후에 하세요.

---

## 3. 명령어

```bash
npm install
npm run dev        # 로컬 http://localhost:8787 (로컬 D1, 배포본과 완전 별개)
npm run deploy     # 배포. 몇 초 걸림
npm run tail       # 배포본 실시간 로그

npm run db:local   # 로컬 DB 에 스키마 적용
npm run db:remote  # 원격 DB 에 스키마 적용
```

**로컬 DB 초기화** (테스트 데이터를 지우고 깨끗하게):

```bash
rm -rf .wrangler/state && npm run db:local
```

`schema.sql` 하나만으로 완전한 DB 가 만들어집니다. `migrations/` 는 **이미 적용된 변경의 기록**이며
새 DB 를 만들 때 다시 돌릴 필요가 없습니다 (`003_seed_sell_price.sql` 만 판매가 시드용으로 선택 적용).

---

## 4. 절대 깨뜨리면 안 되는 것

### D1 에는 대화형 트랜잭션이 없습니다

이게 이 코드베이스에서 가장 중요한 제약입니다.
`BEGIN … COMMIT` 을 쓸 수 없고, 대신 `db.batch([...])` 가 하나의 트랜잭션으로 원자 실행됩니다.

그래서 "읽어서 확인하고 → 쓴다"를 그대로 옮기면 **버튼을 두 번 누를 때 두 번 적용됩니다.**
이 코드는 **조건을 SQL 안에 넣어서** 막습니다.

```js
// 예: 포장 완료. 모든 쓰기가 "아직 포장 전" 조건에 걸려 있다.
const guard = {
  sql: "EXISTS (SELECT 1 FROM orders WHERE id = ? AND status = 'picking')",
  binds: [id],
};
// moveStockStmts() 가 재고 UPDATE 와 원장 INSERT 에 같은 조건을 붙인다.
// 주문 상태 UPDATE 는 batch 의 마지막에 둔다 → 앞의 가드들은 갱신 전 상태를 본다.
```

동시 요청 5건으로 검증했습니다. 하나만 성공하고 재고는 한 번만 빠집니다.
**재고 관련 코드를 고칠 때 이 패턴을 깨뜨리지 마세요.**

### 재고 원장 불변식

```
products.stock === SUM(stock_moves.delta)   -- 항상 참이어야 함
```

재고를 바꿀 때 `UPDATE products SET stock = ...` 를 직접 쓰지 마세요.
반드시 `src/db.js` 의 **`moveStockStmts()`** 를 통해 바꾸세요. 재고 변경과 원장 기록이 같이 일어납니다.

어긋났을 때 복구: 현황 탭의 `재고 재동기화` 버튼 (또는 `POST /api/reconcile`).

### 브랜드 전용 메모는 서버에서 걸러집니다

`readState(db, role)` 이 `role !== "brand"` 일 때 `memoBrand` 키를 **삭제**합니다.
화면에서 숨기는 게 아니라 응답에서 빼는 것이므로, 이 구조를 유지하세요.

### 주문번호는 UNIQUE 입니다

주문을 감출 때(`hidden = 1`) 번호를 그대로 두면 그 번호를 다시 쓸 수 없습니다.
감출 때 `order_no` 에 접미사를 붙여 번호를 풀어주세요. README 의 "주문 감추기" 참고.

---

## 5. 구조

| 파일 | 하는 일 |
|---|---|
| `src/index.js` | 라우팅, API 핸들러, 로그인, 시도 제한 |
| `src/db.js` | D1 조회, 재고 원장(`moveStockStmts`), 세션, 정합성 검사 |
| `src/seed.js` | 빈 DB 일 때 실리콘투 1차 발주분 39품목 314개 주입 |
| `schema.sql` | 전체 스키마 (이것만으로 완전한 DB 생성 가능) |
| `migrations/` | 이미 적용된 변경 기록 (새 DB 에는 불필요) |
| `public/app.js` | 화면 전체. 프레임워크 없음, 함수가 HTML 문자열을 반환하는 방식 |
| `public/index.html` | 뼈대 + `<dialog>` 들 (재고이력·조정·메모·개수점검) |
| `public/styles.css` | 토큰 기반 CSS, 라이트/다크 양쪽 대응 |
| `legacy-node/` | Cloudflare 이전의 단독 Node 버전. 참고용, 배포와 무관 |

**화면은 전역 상태 `S` 를 서버에서 받아 통째로 다시 그립니다.**
`render()` 가 `#root` 의 innerHTML 을 교체하고, 이벤트는 `data-act` 속성으로 위임 처리합니다.
10초마다 `/api/version` 만 확인해서 바뀌었을 때만 전체를 다시 받습니다 (탭이 숨겨져 있으면 안 물어봄).

---

## 6. 도메인 규칙

### 역할

접속 코드로 갈립니다. `brand`(민희님) / `warehouse`(실장님).

실장님이 **할 수 있는 것**: 주문 수락, 포장 완료, 포장 완료한 주문 취소, 재고 조정(사유 필수),
재고·이력 조회, 공유 메모, 개수 점검.
**할 수 없는 것**: 주문 등록, 입고, 제품 등록·보관, 백업, 재동기화, 브랜드 화면 접근.

실장님 화면에는 브랜드 화면으로 가는 길이 아예 없습니다.
민희님만 `실장님 화면 보기` 로 미리 볼 수 있고, 그때는 점선 띠가 뜹니다.

### 주문 상태

```
포장 대기중(new) → [주문 수락] → 포장 중(picking) → [포장 완료] → 포장 완료(shipped)
                                                              ↓ [취소]
                                                          취소(cancelled) — 재고 반환
```

- **수락 전에는 품목 체크가 서버에서 막힙니다** (`pickItem` 이 `status !== 'picking'` 이면 409)
- **전부 담기 전에는 포장 완료가 안 됩니다** (`shipOrder` 가 `picked=0` 인 품목을 검사)
- 재고는 **포장 완료 시점**에 빠집니다

### 재고 세 가지 수치

- **현재고** — 창고 실물. 포장 완료해야 줄어듦
- **출고대기** — 아직 안 나간 주문이 잡아둔 수량
- **가용** — 현재고 − 출고대기

### 되돌리기 (민희님 최우선 가치)

1. 주문 취소 → 포장 완료된 건이면 재고가 창고로 반환
2. 입고 되돌리기 → 그 입고분을 재고에서 뺌. 이미 팔렸으면 거절
3. 재고 재동기화 → 원장 합계로 재계산
4. **D1 Time Travel** → 30일 내 아무 시점으로 DB 통째 되감기, 무료

```bash
npx wrangler d1 time-travel restore isty --timestamp="$(date -u -v-2H +%Y-%m-%dT%H:%M:%SZ)"
```

---

## 7. 작업 방식 (이대로 하면 사고가 안 납니다)

1. **로컬에서 먼저** — `npm run dev` 로 띄우고 curl 로 API 를 직접 검증
2. **엣지 케이스를 API 로 확인** — 권한(403), 중복(409), 재고 부족(409), 동시 요청
3. **배포** — `npm run deploy`
4. **프로덕션 검증** — 실제 주소에 curl 로 확인
5. **로컬 테스트 데이터 정리** — `rm -rf .wrangler/state && npm run db:local`

**프로덕션 데이터를 지우거나 고치기 전에는 반드시 백업하세요.**

```bash
b=$(mktemp); curl -s -c $b -X POST https://isty-stock.isty-stock.workers.dev/api/login \
  -H 'content-type: application/json' -d '{"code":"<브랜드코드>"}' >/dev/null
curl -s -b $b https://isty-stock.isty-stock.workers.dev/api/export > "backups/before-$(date +%Y%m%d-%H%M%S).json"
```

`backups/` 에는 고객 이름·주소가 들어 있어 `.gitignore` 로 제외돼 있습니다.

---

## 8. 함정

- **마이그레이션은 로컬·원격 양쪽에 적용해야 합니다.** 한쪽만 하면 배포 후에 터집니다.
- **`schema.sql` 도 같이 갱신하세요.** 안 하면 새 DB 를 만들 때 컬럼이 빠집니다
  (실제로 `activity.kind` 가 이렇게 빠져 있었습니다).
- **화면 재렌더링 중 입력 포커스가 날아갑니다.** 검색창은 220ms 디바운스 후
  커서 위치를 복원하는 처리가 들어가 있습니다.
- **탭 전환 시 `UI.openOrder` 를 비워야 합니다.** 안 그러면 어느 탭을 눌러도 주문 상세가 남습니다.
- **wrangler 명령이 `wrangler.jsonc` 의 `database_id` 를 참조합니다.** UUID 로 직접 지정할 때도 있습니다.
- **로그인 8회 실패 시 IP 가 10분 잠깁니다.** 테스트하다 잠기면:
  `npx wrangler d1 execute isty --remote --command "DELETE FROM login_attempts" -y`

---

## 9. 열려 있는 것

- [ ] **주문 1003 재고 확인** — 감춘 주문(송장 13834690010024)이 실제 출고였는지 테스트였는지.
      테스트였다면 재고를 306 → 314 로 되돌려야 함
- [ ] **GitHub 비공개 저장소에 push** — 지금은 로컬 git 만 있음. 민감정보는 이미 제외 처리됨
- [ ] 환불 기능 (취소는 있음)
- [ ] 주문 CSV/엑셀 일괄 업로드
- [ ] 판매 채널 연동

---

## 10. 사업 맥락 (숫자 작업을 할 때 필요)

**1차 발주** — 실리콘투 PI00786492 (2026-08-13), 39품목 314개, EUR 1,244.20

**8월 인보이스** — ISTY L0002608, 과세표준 1,685.42 € + IVA 21% 353.94 € = **2,039.36 €**
- 제품 1,493.04 € (실리콘투 도매가 × 1.2 — 3PL 리셀 20%)
- 서비스 192.38 € (보관료 120 € = 창고 2칸, **매달 나감**)

**손익분기 분석 결과 — 현재 구조는 적자입니다.**
배송비 정책(40 € 이상 무료 / 미만 3.95 €)을 반영해도 전 구간 적자이고,
평균 2개/주문 기준 손익분기에 158개가 필요한데 팔 수 있는 건 151개입니다.
자세한 계산은 `~/Documents/Lococo/claude-outputs/2026-09-06-ISTY-인보이스-BEP분석.md` 참고.

주요 논점: 40 € 무료배송 기준선이 너무 낮음 / 가격표의 마스크 3종 원가가 4배 부풀려져 있음
(박스 가격을 낱장 원가로 계산) / **VAT 환급 가능 여부가 손익을 좌우함** (매달 350 € 규모).

가격표: [Google Sheets](https://docs.google.com/spreadsheets/d/1-J1xDo7_F6WBkTaRTUEuWpGUxbQ7TIa7HCdfsoziNF8/edit)

---

## 11. 사용자 화면 설명서

민희님·실장님이 읽는 조작법은 **`README.md`** 에 있습니다.
기능을 바꾸면 README 도 같이 고쳐주세요.
